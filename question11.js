//Declare an array called names
var names = ["Zain", "Hamza", "Sana", "Haris"];
// Print each person's name by accessing each element in the list
for (var i = 0; i < names.length; i++) {
    console.log(names[i]);
}
